# AI Service Configuration
# AI_PROVIDER controls transcription (audio -> text).
# Default: Groq (Free tier, no credit card needed!)
# Alternatives: openai, deepgram, anthropic
AI_PROVIDER=groq

# AI_SUMMARIZATION_PROVIDER controls translate/format/summarize (text generation),
# independently from AI_PROVIDER above. Falls back to AI_PROVIDER if unset.
# Alternatives: openai, groq, anthropic
AI_SUMMARIZATION_PROVIDER=gemini

# Gemini Configuration (used for translate/format/summarize)
# Get your API key: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_SUMMARIZATION_MODEL=gemini-3.6-flash

# Groq Configuration (Recommended for transcription - Fast & Free!)
# Get your API key: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here
GROQ_TRANSCRIPTION_MODEL=whisper-large-v3
GROQ_SUMMARIZATION_MODEL=openai/gpt-oss-20b

# OpenAI Configuration (Alternative - Requires payment)
# OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_TRANSCRIPTION_MODEL=whisper-1
# OPENAI_SUMMARIZATION_MODEL=gpt-4-turbo-preview

# Deepgram Configuration (Alternative)
# DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Anthropic Configuration (for summarization)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# ANTHROPIC_MODEL=claude-3-opus-20240229

# Upload Configuration
MAX_FILE_SIZE_MB=100
ALLOWED_AUDIO_FORMATS=mp3,wav,m4a,aac,ogg,flac
ALLOWED_VIDEO_FORMATS=mp4,mkv,mov,avi,webm

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# FFmpeg Configuration (Optional - Auto-detected if not specified)
# Only set these if auto-detection fails or you want to use a specific installation
# FFMPEG_PATH=/usr/local/bin/ffmpeg
# FFPROBE_PATH=/usr/local/bin/ffprobe
# 
# Common paths:
#   macOS (Intel):     /usr/local/bin/ffmpeg
#   macOS (M1/M2):     /opt/homebrew/bin/ffmpeg
#   Linux:             /usr/bin/ffmpeg
#   Windows:           C:/ffmpeg/bin/ffmpeg.exe
